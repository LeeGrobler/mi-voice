import { StyleSheet, Text, View } from "react-native";

type Props = {};

const Upsert = (props: Props) => {
  return (
    <View>
      <Text>Upsert</Text>
    </View>
  );
};

export default Upsert;

const styles = StyleSheet.create({});
